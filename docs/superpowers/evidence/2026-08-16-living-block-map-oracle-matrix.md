# Living Block Map Old-to-Native Oracle Matrix

**Source snapshot:** `C:\\Users\\htper\\core-ai-wcus` at the commit recorded in `tests/fixtures/living-block-map-source-assets.json`.

Each row maps all assertions contained by the named source test. This makes every legacy assertion traceable at its test boundary while keeping related expectations—markup, geometry, ARIA, timing, or worker effects—together. Allowed dispositions are `ported`, `intentional-context-change`, and `wordpress-only`.

## `normalize.test.js`

| Source test (all contained assertions)                             | Disposition    | Native contract          | Reason                                                                                                                  |
| ------------------------------------------------------------------ | -------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------- |
| upgrades only exact legacy welcome defaults                        | wordpress-only | MAP-22 effective fixture | Saved-block editor migrations are not part of a fixed English Astro exhibit; only the effective live values are ported. |
| keeps the current MCP transport version when upgrading v3.2.0 copy | wordpress-only | MAP-22 effective fixture | Saved-block editor migrations are not part of a fixed English Astro exhibit; only the effective live values are ported. |
| upgrades untouched v0.2 defaults to the v3.1.1 editor copy         | wordpress-only | MAP-22 effective fixture | Saved-block editor migrations are not part of a fixed English Astro exhibit; only the effective live values are ported. |
| restores new items and canonical product-owned fields              | wordpress-only | MAP-22 effective fixture | Saved-block editor migrations are not part of a fixed English Astro exhibit; only the effective live values are ported. |
| preserves visitor-authored copy that differs from the old default  | wordpress-only | MAP-22 effective fixture | Saved-block editor migrations are not part of a fixed English Astro exhibit; only the effective live values are ported. |
| upgrades untouched pre-booth v3.1.1 architecture and release copy  | wordpress-only | MAP-22 effective fixture | Saved-block editor migrations are not part of a fixed English Astro exhibit; only the effective live values are ported. |
| preserves custom pre-booth copy instead of replacing it            | wordpress-only | MAP-22 effective fixture | Saved-block editor migrations are not part of a fixed English Astro exhibit; only the effective live values are ported. |

## `render-contract.test.js`

| Source test (all contained assertions)                                | Disposition    | Native contract          | Reason                                                                                                                 |
| --------------------------------------------------------------------- | -------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| renders SVG without Interactivity directives or PHP stderr            | ported         | MAP-01, MAP-22           | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| keeps the neutral composition clear of every card intersection        | ported         | MAP-21, VIS parity       | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| keeps Connectors beside the provider request path, not inside it      | ported         | MAP-04–MAP-08            | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| serializes task as the third actor in the agent-learning workflow     | ported         | MAP-04–MAP-08            | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| migrates legacy visitor destinations, QR assets, and MCP labels       | wordpress-only | MAP-22 effective fixture | Legacy/custom block-attribute migration is editor-only; the fixture and model preserve the effective public output.    |
| migrates untouched pre-booth v3.1.1 copy on the server                | wordpress-only | MAP-22 effective fixture | Legacy/custom block-attribute migration is editor-only; the fixture and model preserve the effective public output.    |
| migrates v3.2.0 MCP copy to the registered protocol wording           | wordpress-only | MAP-22 effective fixture | Legacy/custom block-attribute migration is editor-only; the fixture and model preserve the effective public output.    |
| migrates v3.2.1 AI Client and Connectors copy to the verified wording | wordpress-only | MAP-22 effective fixture | Legacy/custom block-attribute migration is editor-only; the fixture and model preserve the effective public output.    |
| keeps the WP-Bench process cues and complete evidence rationale       | ported         | MAP-07, MAP-13           | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| renders an accessible OpenAI Codex transparency dialog                | ported         | MAP-12, MAP-15           | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| keeps the About trigger in the colophon rather than the top bar       | ported         | MAP-12, MAP-15           | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| gives every participant in every flow a role to explain               | ported         | MAP-04–MAP-08            | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| gives every flow a situation, takeaway, outcome, and numbered run     | ported         | MAP-04–MAP-08            | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| presents every card through the same interaction pattern              | ported         | MAP-10                   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| orders native card controls along every numbered flow                 | ported         | MAP-10                   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| opens each panel with the flow it was reached from                    | ported         | MAP-10                   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| keeps exactly one level-one heading, named, in every screen state     | ported         | MAP-15                   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| names both headings from the block default when the title is cleared  | wordpress-only | MAP-22 effective fixture | Legacy/custom block-attribute migration is editor-only; the fixture and model preserve the effective public output.    |
| teaches the interaction grammar on the welcome screen                 | ported         | MAP-02                   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |
| labels the flow controls and premises in the open                     | ported         | MAP-04–MAP-08            | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior. |

## `render.test.js`

| Source test (all contained assertions)                                   | Disposition                | Native contract    | Reason                                                                                                                             |
| ------------------------------------------------------------------------ | -------------------------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |
| renders the provider plugin runtime layer and Connectors sidecar         | ported                     | MAP-04–MAP-08      | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| keeps interactive role strips after the contiguous card controls         | ported                     | MAP-10             | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| uses shipped 7.1 language and a per-channel public default               | ported                     | MAP-22             | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| uses a passing muted-text token while preserving decorative gray lines   | ported                     | MAP-01, MAP-22     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| separates configuration strokes and ambient edges from strong boundaries | ported                     | MAP-01, MAP-22     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| renders the explicit WordPress task actor in story 03                    | ported                     | MAP-04–MAP-08      | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| renders the Abilities inspector as three accessible tabs                 | ported                     | MAP-10, MAP-11     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| renders the selectable five-stage WP-Bench run loop                      | ported                     | MAP-07, MAP-13     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| ships accessible QR assets without the hatch fallback                    | ported                     | MAP-22             | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| keeps non-map screens out of the server-rendered tab order               | ported                     | MAP-15             | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| includes progressive disclosure state before Interactivity hydrates      | ported                     | MAP-01, MAP-22     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| renders static preview markers with only the initial workflow visible    | ported                     | MAP-02             | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| renders the AI Plugin review, Apply, note, and Replay controls           | ported                     | MAP-14             | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| uses the corrected actor, parked-card, and touch geometry                | ported                     | MAP-21, VIS parity | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| keeps active outcome text at AA contrast                                 | ported                     | MAP-21, VIS parity | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| renders the inspector hierarchy as named semantic headings               | ported                     | MAP-15             | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| normalizes the reviewed date in the editor as well as the server         | intentional-context-change | MAP-22             | The reviewed date is preserved, while editor normalization is retired.                                                             |
| keeps the take-away QR discoverable below the inspector fold             | ported                     | MAP-10             | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| emits modern and Apple install-capability metadata                       | ported                     | MAP-10, MAP-11     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| skips only the unreadable split-filesystem theme style on the kiosk      | wordpress-only             | MAP-20             | The dedicated Astro route has no WordPress theme stylesheet loader to filter.                                                      |
| keeps the Bench Back control clickable below the persistent brand bar    | ported                     | MAP-07, MAP-13     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| keeps the band under the map legible in both map modes                   | ported                     | MAP-21, VIS parity | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| holds the MCP token choreography in its semantic end state               | ported                     | MAP-01, MAP-22     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| does not register a site-wide service-worker scope                       | intentional-context-change | MAP-23, MAP-20     | The plugin worker is replaced by the site's v4 network-first/stale-while-revalidate worker; unsupported offline status is removed. |
| normalizes versioned cache keys and precaches built local fonts          | intentional-context-change | MAP-23, MAP-20     | The plugin worker is replaced by the site's v4 network-first/stale-while-revalidate worker; unsupported offline status is removed. |
| validates complete offline batches and updates assets network-first      | intentional-context-change | MAP-23, MAP-20     | The plugin worker is replaced by the site's v4 network-first/stale-while-revalidate worker; unsupported offline status is removed. |
| never intercepts or caches arbitrary same-origin API responses           | intentional-context-change | MAP-23, MAP-20     | The plugin worker is replaced by the site's v4 network-first/stale-while-revalidate worker; unsupported offline status is removed. |
| discovers extra static assets only on public anonymous kiosk pages       | ported                     | MAP-01, MAP-22     | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.             |
| marks cached navigation HTML without a failing client-side probe         | intentional-context-change | MAP-23, MAP-20     | The plugin worker is replaced by the site's v4 network-first/stale-while-revalidate worker; unsupported offline status is removed. |
| caches the canonical permalink rather than a query-string variant        | intentional-context-change | MAP-23, MAP-20     | The plugin worker is replaced by the site's v4 network-first/stale-while-revalidate worker; unsupported offline status is removed. |
| isolates exact kiosk scopes and never precaches authenticated HTML       | intentional-context-change | MAP-23, MAP-20     | The plugin worker is replaced by the site's v4 network-first/stale-while-revalidate worker; unsupported offline status is removed. |

## `scope.test.js`

| Source test (all contained assertions)                                              | Disposition    | Native contract | Reason                                                                                                  |
| ----------------------------------------------------------------------------------- | -------------- | --------------- | ------------------------------------------------------------------------------------------------------- |
| keeps a directory-style permalink inside its scope                                  | wordpress-only | MAP-23          | Signed per-permalink plugin worker scopes and PHP query parsing are removed with the WordPress runtime. |
| disables offline mode for suffix permalinks that cannot have a safe directory scope | wordpress-only | MAP-23          | Signed per-permalink plugin worker scopes and PHP query parsing are removed with the WordPress runtime. |
| rejects an encoded dot-segment worker scope                                         | wordpress-only | MAP-23          | Signed per-permalink plugin worker scopes and PHP query parsing are removed with the WordPress runtime. |
| accepts a valid signed percent-encoded non-Latin kiosk scope unchanged              | wordpress-only | MAP-23          | Signed per-permalink plugin worker scopes and PHP query parsing are removed with the WordPress runtime. |
| rejects array-shaped public query parameters without a PHP error                    | wordpress-only | MAP-23          | Signed per-permalink plugin worker scopes and PHP query parsing are removed with the WordPress runtime. |

## `qr-assets.test.js`

| Source test (all contained assertions)                                 | Disposition | Native contract                        | Reason                                                                               |
| ---------------------------------------------------------------------- | ----------- | -------------------------------------- | ------------------------------------------------------------------------------------ |
| commits seven deterministic SVG QR codes with a four-module quiet zone | ported      | source provenance test; MAP-10; MAP-22 | The seven QR payloads and exact SVG bytes are copied and hash-locked as Vite assets. |

## `view.test.js`

| Source test (all contained assertions)                                                         | Disposition                | Native contract | Reason                                                                                                                          |
| ---------------------------------------------------------------------------------------------- | -------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| removes covered theme chrome from the accessibility tree while mounted                         | intentional-context-change | MAP-01, MAP-18  | A dedicated route renders no site header/footer, so sibling isolation is unnecessary and deliberately absent.                   |
| synchronizes SVG state markers from the final kiosk effect                                     | ported                     | MAP-01, MAP-22  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| opens directly into the first flow and focuses step one                                        | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| opens the component explorer from the welcome browse control                                   | ported                     | MAP-02          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| names the state the visitor is in, one instruction at a time                                   | ported                     | MAP-01, MAP-22  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| shows the complete teaching state immediately with reduced motion                              | ported                     | MAP-16          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| cancels a stale conclusion when another flow is selected                                       | ported                     | MAP-04–MAP-08   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| announces a conclusion once after an early inspector visit                                     | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| announces a pending conclusion after returning from About                                      | ported                     | MAP-12, MAP-15  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| lets a flow highlight only the cards that take part in it                                      | ported                     | MAP-10          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| makes every component tappable and uncued in the explorer                                      | ported                     | MAP-09          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| keeps every operable card fully opaque on the neutral map                                      | ported                     | MAP-09          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| keeps off-flow actors and the provider plugin visible, dimmed, and inert                       | ported                     | MAP-04–MAP-08   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| keeps Connectors as an unnumbered configuration sidecar                                        | ported                     | MAP-04–MAP-08   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| teaches the same provider path in the attract preview                                          | ported                     | MAP-02          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| holds positions and dims non-members when recomposition is off                                 | ported                     | MAP-01, MAP-22  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| lights only the boundary rules a story actually crosses                                        | ported                     | MAP-04–MAP-08   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| keeps post-engagement paths visible but only animates during the one-shot transition           | ported                     | MAP-01, MAP-22  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| shows role strips for story members and for the open block                                     | ported                     | MAP-10          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| applies the AI Plugin suggestion and advances it when the story replays                        | ported                     | MAP-14          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| does not turn Escape into an undocumented map reset                                            | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| retains the selected story through the inspector and returns focus to its card                 | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| opens the AI transparency dialog and restores focus to its trigger on Escape                   | ported                     | MAP-12, MAP-15  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| replays rather than clears the flow already showing                                            | ported                     | MAP-04–MAP-08   | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| replay returns keyboard focus to the first numbered step                                       | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| cancels a pending conclusion when the visitor resets                                           | ported                     | MAP-01, MAP-22  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| keeps the flow and restores focus when a panel closes                                          | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| returns to the attract screen and focuses its prompt                                           | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| resets after the configured inactivity timeout                                                 | intentional-context-change | MAP-17          | Reset is preserved only for explicit ?kiosk=1; the public route has no time limit.                                              |
| waits for the scoped service worker before asking it to cache an anonymous page                | intentional-context-change | MAP-23, MAP-20  | Plugin-worker lifecycle and cache-result messaging are replaced by the site worker; the offline badge is intentionally removed. |
| shows offline state when the service worker marks cached HTML                                  | intentional-context-change | MAP-23, MAP-20  | Plugin-worker lifecycle and cache-result messaging are replaced by the site worker; the offline badge is intentionally removed. |
| cleans up the same kiosk post worker after its permalink changes                               | intentional-context-change | MAP-23, MAP-20  | Plugin-worker lifecycle and cache-result messaging are replaced by the site worker; the offline badge is intentionally removed. |
| retires the previous permalink worker before enabling the new scope                            | intentional-context-change | MAP-23, MAP-20  | Plugin-worker lifecycle and cache-result messaging are replaced by the site worker; the offline badge is intentionally removed. |
| cycles attract previews while interactive story selection remains empty                        | ported                     | MAP-02          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| walks each attract preview through assemble, draw, signal, and settled phases before advancing | ported                     | MAP-02          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| keeps the active attract preview composed with its path and caption under reduced motion       | ported                     | MAP-16          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| removes hidden screens and the story rail from the tab order                                   | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| selects Abilities detail tabs through context state                                            | ported                     | MAP-10, MAP-11  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| uses roving focus for the Abilities tabs with arrow, Home, and End keys                        | ported                     | MAP-10, MAP-11  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| scopes global keyboard handling to the map that owns the event target                          | ported                     | MAP-23, MAP-20  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| restores focus to the WP-Bench card after opening the run loop from its inspector              | ported                     | MAP-07, MAP-13  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| announces a clean component title when opening details                                         | ported                     | MAP-15          | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| cancels the Bench flow timer when Escape closes the run loop                                   | ported                     | MAP-07, MAP-13  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| opens WP-Bench at the sandbox and selects its evidence stage                                   | ported                     | MAP-07, MAP-13  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |
| changes WP-Bench detail without replaying the run-loop path animation                          | ported                     | MAP-07, MAP-13  | The native model, markup, state, controller, browser contracts, and parity captures preserve this observable behavior.          |

## Deletion gate

The source suites and repository remain untouched through implementation, browser verification, VIS parity, deployment, and the required 24-hour soak. This matrix authorizes no deletion; it only records the approved native destination or the reason an old assertion is retired.
